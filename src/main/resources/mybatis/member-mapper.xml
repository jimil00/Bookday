<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Member">

	<select id="nickCheck" resultType="kh.bookday.dto.MemberDTO">
  		select nickname from member where nickname = #{value}
  	</select>
  	
  	<select id="phoneCheck" resultType="kh.bookday.dto.MemberDTO">
  		select phone from member where phone = #{value}
  	</select>
  	
  	<select id="isLoginOk" resultType="kh.bookday.dto.MemberDTO">
  		select phone, pw from member where phone = #{phone} and pw=#{pw}
  	</select>
  	<insert id="insert">
  	insert into member values(#{id},#{phone},#{pw},#{nickname},#{email},0,0,0,sysdate,0,0)
  	</insert>
  	
  	<select id="selectIdPwByPhone" resultType="kh.bookday.dto.MemberDTO">
  		select id, pw from member where phone = #{value}
  	</select>
  	
  	<update id="updatePw">
  		update member set pw=#{pw} where phone=#{phone}
  	</update>


</mapper>