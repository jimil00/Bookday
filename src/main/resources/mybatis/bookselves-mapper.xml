<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Bookselves">

	<select id="selectOnesRentalBooks" resultType="kh.bookday.dto.RentalDTO">
		select * from rental where id = #{id}
	</select>
	
	<select id="selectOnesWishlistBooks" resultType="kh.bookday.dto.WishlistDTO">
		select * from wishlist where id = #{id}
	</select>
</mapper>